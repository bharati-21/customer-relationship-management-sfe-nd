import { StyleSheet } from "react-native";

const customerStyles = () =>
	StyleSheet.create({
		heading: {
			textAlign: "center",
			fontSize: 24,
			fontWeight: "bold",
			marginBottom: 20,
		},
	});

export { customerStyles };
